import css from 'styled-jsx/css'

export default css`
  button {
    width: 30px;
    border: none;
    height: 30px;
    cursor: pointer;
    border-radius: 10px;
    font-size: 1.25rem;
    color: var(--font-main-color);
    background: var(--second-color);
  }
`
